/**
 * Captured `GET /resources/detected_field/<name>/values` responses for
 * `{service_name="nginx-json"}`. Empty until captured — `getMockDetectedField
 * Values` will return an empty `values` array for this service.
 */
export const fieldValues: Record<string, string[]> = {};
