/**
 * Captured `GET /resources/label/<name>/values` responses for
 * `{service_name="nginx-json"}`. Empty until captured — `getMockLabelValues`
 * will return an empty `data` array for this service.
 */
export const labelValues: Record<string, string[]> = {};
