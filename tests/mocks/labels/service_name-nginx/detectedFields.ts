/**
 * Captured `GET /resources/detected_fields` response for
 * `{service_name="nginx"}`.
 */
export const detectedFields = {
  fields: [
    {
      label: 'detected_level',
      type: 'string',
      cardinality: 4,
      parsers: null,
    },
  ],
  limit: 1000,
} as const;
